export const sampleDocument = `# Grain, but with taste

Welcome to the first real foundation for this editor.

## What this app should become

- Fast enough for everyday writing
- Flexible enough to shape around your workflow
- Beautiful enough that the default theme already feels intentional

> Grain should disappear when you write and feel powerful when you customize.

### A few starter patterns

You can write inline code like \`const focused = true\` and fenced blocks:

\`\`\`ts
type LayoutMode = 'editor' | 'split' | 'preview';

function describeMode(mode: LayoutMode) {
  return mode === 'split' ? 'Write and review side by side.' : 'Stay in flow.';
}
\`\`\`

And you can sketch structure quickly:

1. Capture the idea
2. Shape the draft
3. Refine the voice

### Everyday checklist

- [x] Restore the last draft
- [x] Switch layouts without losing context
- [ ] Add command palette and export presets

---

| Goal | Why it matters |
| --- | --- |
| Live preview | Keep the feedback loop tight |
| Themes | Make the tool feel personal |
| Autosave | Never lose momentum |
`;
